<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Leave Request Form</title>
    <script>
        function setDate() {
            var nowDate = new Date(); 
            var date = nowDate.getFullYear()+'-'+(nowDate.getMonth()+1)+'-'+nowDate.getDate()
            var input = document.createElement('input')
            input.type = "hidden"
            input.name = "day"
            input.value = date
            const form = document.leaveForm
            form.appendChild(input)
        }
    </script>
</head>
<body>
    <form name="leaveForm" method="POST" onsubmit="setDate()">
        
        <label>Leave From:<input type="date" name="startDate"></label>
        <br>
        <label>Leave To:<input type="date" name="endDate"></label>
        <br>
        <label>Reason for Leave:<textarea name="reason"></textarea></label>
        <br>
        <input type="submit" value="Submit">

    </form>
</body>
</html>